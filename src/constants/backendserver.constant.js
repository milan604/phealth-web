export const baseURL = {
  // Create React App Sets Environment as production
  // TODO: Need to change for staging when we have separate staging environment
  URL: process.env.REACT_APP_API_URL,
  PORT: process.env.REACT_APP_API_PORT,
};
